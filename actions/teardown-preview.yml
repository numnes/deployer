# Copie para o repositório da aplicação: .github/workflows/teardown-preview.yml
# Secrets: DEPLOYER_API_URL, DEPLOYER_API_KEY (mesmos do deploy)
name: Teardown preview (deployer)

on:
  pull_request:
    types: [closed]
    branches:
      - master
      - homologation

jobs:
  teardown-preview:
    runs-on: ubuntu-latest
    steps:
      - name: Remover instância de preview
        env:
          DEPLOYER_API_URL: ${{ secrets.DEPLOYER_API_URL }}
          DEPLOYER_API_KEY: ${{ secrets.DEPLOYER_API_KEY }}
          PROJECT_SLUG: ${{ vars.DEPLOYER_PROJECT_SLUG }}
        run: |
          if [ -z "$DEPLOYER_API_URL" ] || [ -z "$DEPLOYER_API_KEY" ]; then
            echo "Defina os secrets DEPLOYER_API_URL e DEPLOYER_API_KEY no repositório."
            exit 1
          fi
          if [ -z "$PROJECT_SLUG" ]; then
            echo "Defina a variável DEPLOYER_PROJECT_SLUG no repositório."
            exit 1
          fi
          BRANCH="${{ github.head_ref }}"
          curl -fsS -X POST "${DEPLOYER_API_URL%/}/deploy/destroy" \
            -H "Content-Type: application/json" \
            -H "X-Deployer-Api-Key: ${DEPLOYER_API_KEY}" \
            -d "{\"project\":\"${PROJECT_SLUG}\",\"branch\":\"${BRANCH}\"}"
